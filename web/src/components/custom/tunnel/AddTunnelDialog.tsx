import { Button } from '@/components/ui/button';

/** 添加隧道 Dialog — 占位组件，后续 Step 5 接入真实 API */
export function AddTunnelDialog() {
  // TODO: Step 5 时实现表单交互 + useCreateTunnel mutation
  return (
    <Button variant="default" className="shadow-sm shadow-primary/20">
      添加隧道
    </Button>
  );
}
